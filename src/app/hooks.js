import { useDispatch, useSelector } from 'react-redux';

// Thin re-exports — handy seams if typing or middleware is added later.
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;
